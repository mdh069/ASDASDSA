import random

linguagens = ["javascript", "python", "dart", "kotlin", "c++", "swift", "Objective-C", "Ruby", "c#", "PHP"]

def sorteio_megasena():
    equipes = []
    for i in range(10):
        numero_linguagem = random.randrange(linguagens.len-1)
        equipes.append({f"equipe {i} linguagem: " {random.randrange(linguagens.len-1)}})
        linguagens.
print(sorteio_megasena())
